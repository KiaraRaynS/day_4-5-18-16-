#import list of words here
#from word_generator import generate_random_word
possible_word = []
correct_word = random.choice(possible_word)

def get_user_response():
    user_answer = input("Guess a letter: ")
    return user_answer

def hang_man_game():
    count = 0
    correct_guesses = []
    incorrect_guesses = []

    while count <=10:
    word_length = len(correct_word)
    get_user_response()

    if user_answer == letter in correct_word:
        letter_location = correct_word.index(letter)
        correct_guesses[letter_location].append(user_answer)
        print("Correct guess!")

        #print letter in correct index, replace _ with letter
        #print remaining _
    else:
       print("Sorry, that is incorrect")
       incorrect_guesses.append(user_answer)
       count += 1

